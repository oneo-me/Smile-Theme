{
    "version": "2.0.0",
    "type": "shell",
    "tasks": [
        {
            "label": "build",
            "command": "clear && go",
            "args": ["run", "builder/*.go"]
        },
        {
            "label": "test",
            "command": "clear && go",
            "args": ["run", "builder/*.go", "test"]
        }
    ]
}
